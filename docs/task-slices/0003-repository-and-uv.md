# Slice 0, task 0.1 — done

# Repository and environment with `uv`

- Project name: `medication-log-api`
- Repository created with `uv init`, published on GitHub, public
- Virtual environment created by `uv`
- Python version pinned in `.python-version`: 3.14.7
- `README.md` filled with basic content
- MIT license added (`LICENSE`)

**What this locks in for the rest of the project:** a versioned, reproducible
base, with name and Python version pinned before any dependency — changing
either now is cheap; after Django is installed, it would not be.
