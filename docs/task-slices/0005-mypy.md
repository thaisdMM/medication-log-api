# Slice 0, task 0.3 — done

# `mypy` as static type checker

- `mypy` installed as a dev dependency (`uv add --dev mypy`), version 2.3.1
- Configuration in `pyproject.toml`, `[tool.mypy]`: `python_version = "3.14"`,
  `files = ["src"]`, `strict = true`
- No `mypy.ini` or `.mypy.ini` file created
- `uv run mypy` — passed, 1 file checked
- `uv run ruff check .` and `uv run ruff format --check .` — still pass
- `.mypy_cache` and `.ruff_cache` left out of the project's `.gitignore` —
  each tool already ignores its own cache directory internally

**What this locks in for the rest of the project:** strict type checking
wired in while the project is still one file — a type error surfaces on
write, not later in bulk.

**Carried into later tasks:**
* `django-stubs`'s `[compatible-mypy]` extra may change the installed `mypy`
  version — task 0.5
* Adding the test directory to the `files` list — task 0.8
* Whether commit messages use a scope (`chore(mypy):`) — task 0.4
